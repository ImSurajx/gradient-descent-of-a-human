# if-statement: condition is true the indented block runs, if it's false python skip it.
marks = 75
if marks >= 40:
    print("you have passed.")
# if marks were 30, nothing would print at all.
# indentation is not optional in python. the block inside if must be indented usually 4 space, missing indentation will give you an indentation error.